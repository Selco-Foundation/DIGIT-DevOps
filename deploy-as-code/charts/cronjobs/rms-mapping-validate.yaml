apiVersion: batch/v1
kind: CronJob
metadata:
  name: rms-mapping-validate
  namespace: core-dev
spec:
  schedule: "0 15 * * 0"
  timeZone: Asia/Kolkata
  successfulJobsHistoryLimit: 1
  suspend: true
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: rms-mapping-validate
            image: selcohub/rms-mapping-validate:latest-14012026
          restartPolicy: OnFailure
