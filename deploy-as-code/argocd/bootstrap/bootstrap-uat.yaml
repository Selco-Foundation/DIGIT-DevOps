apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops-platform-uat
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Selco-Foundation/DIGIT-DevOps.git
    targetRevision: Selco-UAT
    path: deploy-as-code/argocd/platform

    helm:
      valueFiles:
        - values-uat.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: core

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
